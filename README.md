# rust-std-btree

The BTree / BTreeMap implementation extracted from the Rust standard library's `alloc` crate.

Pull a specific version:

```sh
make pull-btree TAG=1.92.0
```

Then `cargo build` (or `make build`).
